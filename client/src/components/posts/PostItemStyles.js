import { makeStyles } from "@material-ui/core";

export default makeStyles(() => ({
    media: {
        height: 0,
        paddingTop: '56.25%',
    },
    lowerSection: {
        display: 'flex',
        backgroundColor: "#333",
        color: "white",
        justifyContent: "space-between",
    }
}))