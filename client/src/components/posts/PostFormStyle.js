import { makeStyles } from "@material-ui/core"

export default makeStyles((theme) => ({
    uploadButton: {
        margin: "10px auto"
    },
    cardContainer: {
        paddingTop: "2rem",
    },
    description: {
        marginTop: "10px"
    }

}))